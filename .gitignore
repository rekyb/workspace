# Re-hydrated by bootstrap.sh from `manifest` — never tracked here.
/brain/
/archives/
/repo/

# Obsidian: keep shared config, drop noisy per-device UI state.
.obsidian/workspace.json
.obsidian/workspace-mobile.json

# OS / editor cruft
.DS_Store
*.swp

# Permission allowlist is intentionally tracked here so it travels with the
# clone — override any global ignore of settings.local.json for THIS repo.
!.claude/settings.local.json

# Scratch
/scratch/
/.superpowers/
/.worktrees/
